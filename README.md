# OCamlEvaluator
## Build
### Prerequisites
* ocaml
* opam
* ocamlfind
* makefile

Execute ```eval $(opam env)```.
### Build executable
Run ```make``` to compile the source, and it will create a "oeval.native" executable

## Usage
